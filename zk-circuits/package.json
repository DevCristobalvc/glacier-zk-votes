{
  "name": "glacier-zk-circuits",
  "version": "1.0.0",
  "description": "ZK circuits for Glacier - Anonymous voting system",
  "type": "module",
  "scripts": {
    "compile": "node scripts/compile.js",
    "setup": "node scripts/setup.js",
    "prove": "node scripts/prove.js",
    "verify": "node scripts/verify.js",
    "build": "npm run compile && npm run setup",
    "clean": "rm -rf build/* && rm -rf ptau/*",
    "test": "node scripts/test.js"
  },
  "dependencies": {
    "snarkjs": "^0.7.4",
    "circomlib": "^2.0.5",
    "ffjavascript": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "keywords": [
    "circom",
    "zk-snarks",
    "zero-knowledge",
    "avalanche"
  ]
}